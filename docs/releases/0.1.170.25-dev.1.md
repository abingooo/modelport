# ModelPort 0.1.170.25-dev.1

## OAuth 安全修复

- 阻止未完成所有权校验的 pending OAuth 登录流程把第三方身份绑定到既有账号。
- 非终态账号选择只返回下一步状态，不修改目标账号资料，也不会消费待处理会话。
- 已完成登录和已认证用户主动绑定 OAuth 的正常流程保持不变。

## 验证

- 新增账号接管攻击回归测试，覆盖身份未绑定、用户资料未修改和会话未消费。
- pending OAuth 交换流程定向测试和 Go race 测试通过。
- 正式发布前仍需通过完整后端、前端和测试服务器验证。
