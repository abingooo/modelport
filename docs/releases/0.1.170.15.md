# ModelPort 0.1.170.15

## 发布门禁修复

- 修复指令审核 V2 在 `golangci-lint v2.9` 下发现的资源关闭、测试断言和静态检查问题，并删除未使用的内部遗留符号。
- 本次调整不改变 `instructions`、`input[1]`、哈希匹配、分组与客户端范围、AI 二审或拦截响应的运行行为。
- 将前端传递依赖 `nanoid` 从 `3.3.16` 固定升级到已修复的 `3.3.18`，消除 `GHSA-2v37-7h3g-55p8` 对应的高危安全扫描告警。

## 数据与部署

- 不新增数据库迁移，升级后迁移数仍为 270；不修改用户、余额、API Key、账号、分组、订阅、用量或计费数据。
- 指令审核 V2 的现有配置和数据保持不变；新安装仍默认以 `off` 模式和空审核范围启动。
- 正式版本仅构建 `linux/amd64`，发布为 `ghcr.io/abingooo/modelport:custom-v0.1.170.15`。
- 生产服务器只显示站内人工更新候选，不执行主动更新；正式镜像先部署到测试服完成复验。
